`ifndef ${fname_prefix}__SV
`define ${fname_prefix}__SV


interface ${name}_intf #(
  // parameter
) (
    input clk,
    input rst_n
);

  // interface pins

  // debug signals

endinterface  //${name}_intf
`endif
